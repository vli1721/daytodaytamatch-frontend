extends template

block content
    .content
        .choice-block
            #buttons
                // child buttons
            #path
                // button onclick('render(node)')
    .modal#found-users
        .modal-content#found-users-content
block postscript
    script.
        renderTree('tree[\'start\']')
