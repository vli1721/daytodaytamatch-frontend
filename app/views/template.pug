doctype html
html
  head
    title= title || "CHANGE TITLE IN TEMPLATE.PUG"
    link(rel='stylesheet', href='/stylesheets/style.css')
    script(src='/js/fetch.js')
  body
    block content
    script(src='/js/script.min.js')
    if token
        script localStorage.token = "#{token}"
